{# Brutalist icon dispatcher (v0.3.13 substrate split). Routes to the dark
   partial (genome surface fill + accent ring + accent-dim glyph — correct on
   dark) or the light partial (ink-mass disc/square + paper knockout glyph +
   accent spot). Both consume the same icon_geometry tokens; only fill routing
   differs. Mirrors the strip/stats/chart substrate-dispatch pattern. Default
   "dark" preserves byte-equal output for non-substrate-axis genomes. #}
{% include "frames/icon/brutalist-" ~ (substrate_kind | default('dark')) ~ "-content.j2" %}
