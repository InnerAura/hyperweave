{# Brutalist STATS paradigm DISPATCHER (v0.3.2 substrate split).

   Resolves substrate_kind from the genome's active variant_override and
   includes the substrate-specific stat-card composition. substrate_kind
   defaults to "dark" so legacy stat cards render identically to pre-v0.3.2
   output; light scholar variants opt in via their substrate_kind="light"
   declaration in brutalist.json variant_overrides.

   Polymorphism via slug interpolation per Invariant 12. #}
{% include "frames/stats/brutalist-" ~ (substrate_kind | default('dark')) ~ "-content.j2" %}
