{# Brutalist BADGE paradigm DISPATCHER (v0.3.2 substrate split).

   Includes the substrate-specific content partial based on resolved
   `substrate_kind` (sourced from genome variant override; see
   compose/context.py). substrate_kind defaults to "dark" so genomes
   without variant overrides (chrome, automata, telemetry-*) continue
   rendering through the dark path with no behavior change. Light
   variants (pulse, archive, depth, signal, afterimage, primer) opt in
   via their variant_overrides declaration of substrate_kind="light".

   Polymorphism via slug interpolation per Invariant 12; no `if
   substrate_kind == "..."` conditionals here or in Python. #}
{% include "frames/badge/brutalist-" ~ (substrate_kind | default('dark')) ~ "-content.j2" %}
