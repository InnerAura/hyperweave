{# OFFLINE — a recessed surface well + dim muted core, no motion (the light is
   off). Baked genome-derived literals for renderer independence. #}
<circle r="{{ status_glyph.housing_r }}" fill="{{ _genome_raw.surface_0 }}" stroke="{{ _genome_raw.stroke }}" stroke-width="{{ status_glyph.housing_stroke_w }}"/>
<circle r="{{ status_glyph.core_r }}" fill="{{ _genome_raw.badge_status_offline }}"/>
