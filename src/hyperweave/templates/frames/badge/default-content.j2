{# Badge "default" paradigm — alias to the brutalist partial.

   The default brutalist look (flat, two-panel, sharp separator, square status
   indicator) is HyperWeave's canonical aesthetic and is kept under
   `brutalist-content.j2` to preserve specimen fidelity. This alias lets
   genomes declare `paradigms.badge = "default"` without renaming the source. #}
{% include "frames/badge/brutalist-content.j2" %}
