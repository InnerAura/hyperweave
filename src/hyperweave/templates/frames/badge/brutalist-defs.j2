{# Brutalist badge defs: clip path only. Value zone uses a flat var(--dna-surface-deep)
   fill in brutalist-content.j2 — brutalist Μ mandates zero gradients on data surfaces. #}
<clipPath id="{{ uid }}-clip">
  <rect width="{{ width }}" height="{{ height }}"/>
</clipPath>
