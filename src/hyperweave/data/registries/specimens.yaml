# Specimen registry — categorized assets served via /a/inneraura/{category}/{slug}.
# Each category dispatches to its own renderer in serve/app.py.

dividers:
  block:     { divider_variant: block,     width: 700, height: 80 }
  current:   { divider_variant: current,   width: 700, height: 40 }
  takeoff:   { divider_variant: takeoff,   width: 700, height: 100 }
  void:      { divider_variant: void,      width: 700, height: 40 }
  zeropoint: { divider_variant: zeropoint, width: 700, height: 30 }
